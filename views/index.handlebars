<main class="container">
  <section id="menu" class="col-6">

    <ul>
      {{#each burgers}}
      <li>
        <p>Title: {{this.title}}</p>
        <button data-id="{{this.id}}" class="devour">Devour</button>
      </li>
      {{/each}}
    </ul>

  </section>
  <section id="devoured" class="col-6">

    <ul>
      {{#each burgers}}
      <li>
        <p>Title: {{this.title}}</p>
      </li>
      {{/each}}
    </ul>

  </section>
</main>